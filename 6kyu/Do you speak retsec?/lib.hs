module Codewars.Kata.Retsec where

reverseByCenter :: String -> String
reverseByCenter "" = ""
reverseByCenter xs
    | odd len   = splitAt (l 
    | otherwise = 
        where
            len           = length xs
            (left, right) = splitAtcd